## Objectives: 
1. Build the blog's DRF API.
2. Containerize + set up the workflow.
   - Move the app from venv into Docker + a Makefile
   - Stand up claude.md for the repo
   - make up runs the whole stack in Docker
3. Deploy the API to Heroku.
4. Vibe coding the React frontend with Claude.
5. Connect React to the API (CORS, auth), Deploy the frontend.

## Reused files from blog_week2
- admin.py
- models.py
- queries.py
